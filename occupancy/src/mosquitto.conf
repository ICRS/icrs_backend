# Listen on all available interfaces (0.0.0.0)
listener 1883 0.0.0.0

# Allow anonymous connections (for testing purposes, consider adding authentication in production)
allow_anonymous true

# Increase message size limit if needed
max_inflight_messages 100
max_queued_messages 1000

# Logging (optional)
log_dest stdout
