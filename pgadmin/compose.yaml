services:
  pgadmin:
    image: dpage/pgadmin4:latest
    restart: always
    ports:
      - 80
    environment:
      PGADMIN_DEFAULT_EMAIL: user@email.com
      PGADMIN_DEFAULT_PASSWORD: password
      SCRIPT_NAME: /pgadmin
